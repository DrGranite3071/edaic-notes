name: Build and publish LaTeX PDFs

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        root_file:
          - main.tex
          - chapters/Peck ch 10 _Analgesics - Opioids.tex

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Compile LaTeX document
        uses: xu-cheng/latex-action@v4
        with:
          root_file: ${{ matrix.root_file }}
          texlive_version: 2024

      - name: Copy PDF into docs/
        run: |
          mkdir -p docs
          root="${{ matrix.root_file }}"
          base=$(basename "$root" .tex)
          dir=$(dirname "$root")

          if [ "$root" = "main.tex" ]; then
            # Full book
            cp main.pdf docs/book_edaic_notes.pdf
          else
            # Chapter PDFs live alongside their .tex: e.g. chapters/ch10_opioids.pdf
            cp "$dir/$base.pdf" "docs/${base}.pdf"
          fi

      - name: Create index page
        if: matrix.root_file == 'main.tex'
        run: |
          cat > docs/index.html << 'EOF'
          <!DOCTYPE html>
          <html>
          <head>
            <meta charset="UTF-8">
            <title>EDAIC Notes PDFs</title>
          </head>
          <body>
            <h1>EDAIC Notes – PDFs</h1>
            <ul>
              <li><a href="book_edaic_notes.pdf">Full book – EDAIC Notes</a></li>
              <li><a href="Peck ch 10 _Analgesics - Opioids.pdf">Chapter 10 – Opioid Analgesic Drugs</a></li>
            </ul>
          </body>
          </html>
          EOF

      - name: Upload artifact for GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
